[project]
name = "server-hub-mcp"
version = "0.2.0"
description = "MCP adapter for the Server Hub REST API"
authors = [{name = "Fábio Tezedor"}]
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.27.0",
]
requires-python = ">=3.10"

[project.scripts]
server-hub-mcp = "mcp_server:run_stdio"
server-hub-mcp-http = "mcp_server:run_http"
